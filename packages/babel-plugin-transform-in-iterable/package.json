{
  "name": "babel-plugin-transform-in-iterable",
  "version": "0.0.9",
  "description": "Babel transform detects use of iterable in binary `in` expression and transforms to indexOf check.",
  "main": "lib/index.js",
  "author": "Gabe Klein <gabe@termtm.com> (https://github.com/gabeklein)",
  "scripts": {
    "build": "babel ./src --out-dir ./lib",
    "dev": "babel ./src --out-dir ./lib --source-maps --watch",
    "prepare": "npm run build"
  },
  "license": "MIT",
  "keywords": [
    "loops",
    "for-in",
    "for-of",
    "iterate",
    "iterator",
    "babel",
    "expressive"
  ],
  "dependencies": {
    "babel-types": "^6.25.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1"
  }
}

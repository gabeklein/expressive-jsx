import { ComponentModifier } from "./attributes";
const { Prop, Style, Statement, ChildNonComponent } = require("./item");

export class GeneralModifier {
    static handle(parent, path){
        const { type } = path;
        if(type == "Labeled"){

        }
    }
}
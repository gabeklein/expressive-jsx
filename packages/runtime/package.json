{
  "name": "@expressive/runtime",
  "version": "1.0.0-beta.65",
  "description": "Expressive-react runtime",
  "main": "lib/index.js",
  "types": "index.d.ts",
  "author": {
    "name": "Gabe Klein",
    "email": "gabe@termtm.com"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/lib",
    "index.d.ts"
  ],
  "scripts": {
    "build": "esbuild ./src --bundle --outfile=lib/index.js --platform=node --target=node10 --external:@babel/types",
    "watch": "npm run build -- --sourcemap --watch",
    "prebuild": "rm -rf ./lib",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "esbuild": "^0.11.14"
  }
}

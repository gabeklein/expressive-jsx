{
	"name": "expressive-react",
	"version": "0.9.0-alpha",
	"description": "An alternative syntax for powerful react constructs using less code",
	"scripts": {
		"start": "concurrently \"npm:watch-core\" \"npm:watch-react\"",
		"watch-core": "npm run watch --prefix packages/plugin-core",
		"watch-react": "npm run watch --prefix packages/plugin-react",
		"clean": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"
	},
	"keywords": [
		"react",
		"expressive",
		"syntax"
	],
	"license": "MIT",
	"devDependencies": {
		"lerna": "^2.8.0"
	},
	"dependencies": {
		"concurrently": "^4.1.0"
	}
}